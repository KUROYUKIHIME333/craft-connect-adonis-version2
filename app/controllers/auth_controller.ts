import type { HttpContext } from '@adonisjs/core/http'
import User from '#models/user'
import argon2 from 'argon2'
import axios from 'axios'

export default class AuthController {
  async registration({}: HttpContext) {
    try {
    } catch (error) {}
  }
}
